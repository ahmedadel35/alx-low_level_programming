#include <stdio.h>

/**
 * crackme_password - prints the chessboard
 * @a: pointer to the 2D array representing the chessboard
 *
 * Return: void
 */
void crackme_password(char (*a)[8])
{
	unsigned int i, j;

	for (i = 0; i < 8; i++)
	{
		for (j = 0; j < 8; j++)
			putchar(a[i][j]);

		putchar('\n');
	}
}

/**
 * main - prints the chessboard
 *
 * Return: 0
 */
int main(void)
{
    putchar('\n');

    return (1);
}
